# dependencies
node_modules/

# build output
dist/

# local runtime state (the live-sync document + generated posters)
.studio/
posters/

# unreleased campaign briefs — kept locally, never published
briefs/campaigns/

# os / editor noise
.DS_Store
*.log
.vite/
